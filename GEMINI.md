Read `AGENTS.md` in the repository root before doing anything else. It is the
single source of truth for this project and contains a task→document table.

Then read the `AGENTS.md` in whichever directory you are editing — `src/app/`,
`src/app/api/`, `src/features/`, `src/lib/`, `src/components/`, or `tests/`.

Do not edit this file. All content lives in `AGENTS.md`.
